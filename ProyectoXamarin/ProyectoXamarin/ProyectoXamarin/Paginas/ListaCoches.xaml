<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoXamarin.Paginas.ListaCoches">

  <Label Text="Lista Coches" VerticalOptions="Center" HorizontalOptions="Center"/>
  <!--<StackLayout Margin="20" VerticalOptions="StartAndExpand">
        <Label Text="Marca" />
        <Entry Text="{Binding Marca}" />
        <Label Text="Modelo" />
        <Entry Text="{Binding Modelo}" />
        <Label Text="Color" />
        <Entry Text="{Binding Color}" />
        <Label Text="Combustible" />
        <Entry Text="{Binding Combustible}" />
        <Label Text="Matricula" />
        <Entry Text="{Binding Matricula}" />
        <Label Text="Pais" />
        <Entry Text="{Binding Pais}" />
        <Button Text="Save" Clicked="OnSaveClicked" />
        <Button Text="Delete" Clicked="OnDeleteClicked" />
    </StackLayout>-->
</ContentPage>